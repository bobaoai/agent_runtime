# Task Instructions

Independently review one frozen System Change Governance candidate. Judge
whether it captures every governed mutation while retaining exactly one owned
object, SystemChangeCase, and taking no semantic, authoring, review, admission,
execution, or release authority from an affected owner.

Evaluate two governed chains and their exact join:

1. intake and realization: system_change_intake, Scope Assessment, authority
   and implementation impacts, accountable decisions, Plan, specialist Work
   Packages, and the candidate-producing Candidate Set; and
2. assurance and completion: subject-specific external results, finding return,
   revision, admission, evidence-only Work Packages, AwaitingCompletion, and
   the Closure Record.

Check that every impact maps to one real owner; structural operations bind a
separately owned peer snapshot, Contract-Audit-owned structure review, and
parent-authority decision; candidate-producing and evidence-only Work Packages
remain distinct; and Case-level materiality cannot downgrade a subject owner's
classification or registered gate.

Apply Design Doc Management Boundary Coherence. Distinguish semantic owner,
author, operator, reviewer, persistence owner, implementation binding, and
approval or admission authority. A Service, Registry, database, framework,
Skill, Reviewer, or current technology cannot silently become semantic owner.

Preserve the two SCG-owned entry preconditions without reviewing the general
Task Routing Registry. Use `topology_judgment.capture_without_ownership` to
judge whether a parentless governed mutation enters `system_change_intake`.
Use `topology_judgment.scope_and_plan` to judge whether specialist branch entry
requires the exact current Case, Plan, and Work Package.

Separate Design defects, pending implementation, and current implementation
defects. Return each finding to the accountable owner and smallest safe
correction. Do not edit the candidate, invent an Audit Profile, copy an
external decision into the Case, select a provider, demand a new authority that
does not follow from the frozen peer set, or treat predecessor implementation
debt as a target-Design defect.

Return only the registered semantic output object.
