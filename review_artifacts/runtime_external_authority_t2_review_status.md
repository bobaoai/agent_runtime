# Runtime External Authority T2: External Review Result

## Result

The final independent external content review covers all eleven DDM check IDs
in canonical order. All ten semantic checks and the subsequent prose check
are satisfied; the findings array is empty. The frozen candidate and all
declared context hashes remained unchanged after review.

Subject: `designDoc/agent_runtime_09_authorization_integration_contract.md`.
SHA-256: `f0cab2805ae900d6b7656d1a626284f9da5ed54830056266ebac09c1961a91c0`.
Plan step: 2, External-authority boundary.
Parsed result: `review_artifacts/runtime_external_authority_t2_external_review.json`.

This is external Design content-review evidence. It is not registered Runtime
Reviewer execution, formal DDM admission, an implementation approval or release
evidence. The candidate remains uncommitted. The local entry decision and exact
accepted T1 prerequisite are recorded in
`review_artifacts/runtime_external_authority_design_handoff.md`.

## Changes and preserved meaning

The existing contract now uses the accepted T2 layer and Runtime T1 parent.
Ordinary Runtime-hosted self-tests bind trusted test resources without requiring
production authorization clients, external contexts or synthetic allow-all
approval. Missing external authority does not select the self-test path.

External protected paths retain exact context validation, declared-operation
and external-decision conjunction, applicable grants, revocation, monotonic
fences, late-result quarantine and non-repeated effects. Portable Product
Authorization retains only its database-permission meaning.

The `the-design-authoring` method also guided the material revision into the
current T2 representation: flow, bounded operations, explicit interfaces and
errors, recovery, peer handoffs and verification. Physical SDK/source inventory
was left to Code Projection and Current Inspection; no SDK migration occurred.

Author self-review covered structure, meaning preservation and prose. The six
Runtime responsibilities and external production safeguards were retained.
No self-review stage was skipped or treated as an independent verdict.

## Review revisions

The first review reported one clock-safety root cause under two DDM checks.
The revision now binds the registered protected-time predicate, applicable
immutable clock profile and fresh per-judgment health evidence, uses the
Timestamp conservative commit-time window, and fails closed when evidence is
unavailable. Health refresh does not enlarge scope or reopen a fence;
revocation remains causally ordered and does not wait for clock health.

The second review identified an unspecified source for required-grant checks.
The final revision makes the external owner's admitted resource contract,
projected through the trusted host adapter, the unique applicability source.
Runtime verifies provenance and exact intent references; missing declarations
cannot default to not-required. The enforcing adapter independently validates
current permission and the actual grant.

The third review independently re-evaluated the full candidate and returned no
findings. Its optional suggestions were not applied to the frozen reviewed bytes.

## Verification

- The existing DDM representation validator passed all five checks for this
  exact T2 candidate.
- Additional temporary operator checks passed: matching front matter/capsule
  identity, eight flow/interface mappings, four error-code definitions and local
  Markdown reference resolution. These are not formal Design Registry checks.
- Diff whitespace validation passed.
- Portable validation, session projection and Reviewer registration-source
  regression: 287 passed in 2.43s. This verifies the existing governance
  deployment, not implementation of the new self-test boundary.

The reported validation results and external reviews were obtained outside the execution sandbox.
Each review used `claude-opus-5` with high effort in a fresh directory, following
an authenticated CLI check and content-free isolation smoke. Initialization
reported empty active tools, MCP servers, Skills and slash commands.

Frozen inputs, predecessor bytes, diffs, raw outputs, checks and isolation evidence:

- `/private/tmp/runtime_external_authority_t2_review.g76uPY/`
- `/private/tmp/runtime_external_authority_t2_r2.DDAoUw/`
- `/private/tmp/runtime_external_authority_t2_r3.jeHw3x/`

## Remaining work

Owner acceptance of this exact candidate precedes plan step 3, the adapter
contract revision. That step removes mandatory production-authorization
evidence from ordinary evaluation and closes the old host-binding dependency.
Capability Verification design follows under step 4.

Runtime code, tests, database state, releases, active pointers, Portable sources
and projections, and the generated Design bundle were not modified by this
step. The accepted T1 bytes remain unchanged. The old host-binding document
has not been archived yet. Clock/predicate and execution implementation
availability are not established by this Design review; required protected
paths remain subject to their actual code bindings and gates.
